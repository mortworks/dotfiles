#!/usr/bin/env bash

# 🚀 Wrapper for hugo-tools/bin/new.sh

# Load tool discovery
source "$(dirname "$0")/_ensure-tools.sh"
source "$(dirname "$0")/_tool-helpers.sh"

TOOL_SCRIPT="$(require_tool hugo-tools/bin/new.sh)" || exit 1

echo "✅ Running Hugo new post tool..."
exec "$TOOL_SCRIPT" "$@"
